<!DOCTYPE html>
<html lang="es">
<%- include('../partials/head'); %>
<body class="text-center">
<main class="form-signin rounded max-w-forgp">
    <form action="/login" method="get" oninput='validatePassword()'>
        <img class="w-25 mb-4 d-flex justify-content-start" src="/images/Logo.png" alt="Free-Lánzate">
        <p class="h5 welcome mb-3 fw-bold">Restablece la contraseña</p>
        <p>Para el usuario<b class="badge">alguien@example.com</b></p>
        <div class="form-floating w-75 mx-auto mt-4 mb-4">
            <input
                    type="password"
                    class="form-control mb-3"
                    id="newPassword"
                    name="newPassword"
                    placeholder="newPassword"
            />
            <label for="newPassword">Contraseña nueva</label>
            <i class="bi bi-eye-slash-fill form-icon" onclick="ShowHide(this)"></i>
        </div>
        <div class="form-floating w-75 mx-auto">
            <input
                    type="password"
                    class="form-control mb-3"
                    id="repeatPassword"
                    name="repeatPassword"
                    placeholder="repeatPassword"
            />
            <label for="repeatPassword">Confirma la contraseña</label>
            <i class="bi bi-eye-slash-fill form-icon" onclick="ShowHide(this)"></i>
        </div>
        <button class="w-75 btn btn-lg btn-primary fw-bold mx-auto mt-3 mb-4" type="submit">Guardar cambios</button>
    </form>
</main>
</body>
</html>